loopenv 1
kind cleanup
rung 3
axis template
parent-tree c4df8e9baadf0303362bcf70314628ac133702ea
corpus ae595a23
rail cost.warnings
--- patch
--- a/src/ai/backward.rs
+++ b/src/ai/backward.rs
@@ -352,7 +352,6 @@
 ///    same treatment, using the live `apply()` as the forward so the test
 ///    exercises the code that will actually train, not a copy of it.
 pub fn selftest() -> bool {
-    use crate::kprintln;
 
     let mut rng = Rng(0x5EED_1234_ABCD_0001);
     let (rows, cols) = (24usize, 16usize);
